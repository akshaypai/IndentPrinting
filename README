A python script prints nested lists in an indented fashion.

The function takes 2 inputs: the nested list and the number of spaces for initial indent. By defaut the printing brgins from the starting of the line.